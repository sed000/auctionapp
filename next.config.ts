import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['placehold.jp', "avatars.githubusercontent.com", "www.ilusso.com"]
  }
};

export default nextConfig;
